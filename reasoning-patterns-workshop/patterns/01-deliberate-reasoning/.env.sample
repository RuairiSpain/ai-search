# Optional overrides. Shared values come from ../../.shared-env after module 0.
# VARIANT=baseline            # default variant for make run / make eval
# FOUNDRY_PROJECT_ENDPOINT=   # override shared project if you must
