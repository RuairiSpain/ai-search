# Double the breadth. Watch whether the recommendation changes vs baseline —
# and what it cost to find out. Budget breach escalates rather than overruns.
generator_deployment: small
scorer_deployment: nano
deep_deployment: small
n_candidates: 16
deepen_top_k: 3
constraint_precheck: true
shield_observations: true
