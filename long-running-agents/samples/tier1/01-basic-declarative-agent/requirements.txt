# Versions intentionally not pinned here -- see the single pin table in
# docs/01-gateway-config-and-adapter-contract.md §3 (reviewed monthly) and
# apply it at install time, e.g.:
#   pip install -r requirements.txt "azure-ai-projects>=2.0.0"
azure-ai-projects
azure-identity
pyyaml
